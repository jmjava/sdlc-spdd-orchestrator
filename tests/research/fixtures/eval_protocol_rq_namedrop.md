# Evaluation protocol

This Work ID does **not collect study data**. Protocol only.

Baselines: unstructured chat, canvas-only, lifecycle-only, full SDLC-SPDD.

Gold task: `examples/spring-boot-order-api/README.md`

Rater protocol, Stop rule, nondeterminism plan.

Spring Boot example has **no Java** sources. Live consumer is Cursor-oriented.

RQ1 RQ2 RQ3 RQ4 RQ5 are in scope but have no procedure headings.
