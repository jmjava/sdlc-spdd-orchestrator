# Security Skill

Before implementing or reviewing:

- Validate inputs at trust boundaries.
- Avoid logging secrets or PII.
- Prefer least-privilege defaults.
- Note authn/authz assumptions in the canvas Safeguards section.

Record `#security` in canvas Metadata when this skill is active.
