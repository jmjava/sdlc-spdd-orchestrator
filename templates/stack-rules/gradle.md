# Gradle Stack Rules

- Use the Gradle wrapper when present (`./gradlew`).
- Do not change Gradle or plugin versions unless the task requires it.
- Prefer existing task names and module structure.
- Document any tasks that could not be run locally.
