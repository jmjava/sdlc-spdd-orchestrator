# Node Stack Rules

- Use the Node version and package manager already configured.
- Follow existing module and test layout.
- Do not add dependencies without canvas justification.
- Run existing test and lint commands when possible.
