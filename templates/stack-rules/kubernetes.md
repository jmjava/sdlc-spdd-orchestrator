# Kubernetes Stack Rules

- Follow existing manifest layout under `k8s/` or `kubernetes/`.
- Do not change cluster-wide resources without explicit approval.
- Document rollout, rollback, and health check implications.
- Prefer minimal manifest diffs.
