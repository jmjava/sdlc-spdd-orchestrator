# Maven Stack Rules

- Use the Maven wrapper when present (`./mvnw`).
- Do not change Maven or plugin versions unless the task requires it.
- Follow existing module and profile structure.
- Document any goals that could not be run locally.
