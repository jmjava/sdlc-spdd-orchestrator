# Python Stack Rules

- Use the Python version and dependency manager already configured.
- Follow existing package and test layout.
- Do not add dependencies without canvas justification.
- Run existing test commands when possible.
